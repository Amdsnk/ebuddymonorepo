{
  "buildCommand": "yarn install && cd packages/shared && yarn build && cd ../../apps/frontend && NEXT_TELEMETRY_DISABLED=1 NODE_OPTIONS='--max_old_space_size=4096' yarn build",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs",
  "installCommand": "yarn install",
  "regions": ["iad1"]
}

