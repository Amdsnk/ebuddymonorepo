{
  "buildCommand": "yarn install && cd packages/shared && yarn build && cd ../../apps/frontend && yarn build",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs",
  "installCommand": "yarn install",
  "regions": ["iad1"]
}

