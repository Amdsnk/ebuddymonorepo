{
  "buildCommand": "yarn install && yarn build:shared && cd apps/frontend && yarn build",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs",
  "installCommand": "yarn install",
  "regions": ["iad1"]
}

